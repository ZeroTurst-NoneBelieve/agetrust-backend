[tool.ruff]
line-length = 120
target-version = "py311"

[tool.ruff.lint]
select = ["E", "W", "F"]

# alembic이 생성한 마이그레이션은 줄 길이·공백을 손대지 않는다.
# 적용된 리비전 파일은 수정하지 않는 것이 이 저장소의 규칙이라
# 스타일 규칙만 빼고 F(정의되지 않은 이름 등)는 그대로 검사한다.
[tool.ruff.lint.per-file-ignores]
"alembic/versions/*.py" = ["E501", "W291"]
